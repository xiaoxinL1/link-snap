# LinkSnap - AI智能链接总结 · 产品需求文档

| 版本 | 日期       | 作者 | 变更说明     |
|------|------------|------|--------------|
| v1.0 | 2026-05-22 | -    | 初始版本创建 |

---

## 1. 项目概述

### 1.1 项目背景
用户每天接收大量网页链接（文章、新闻、报告、博客），但缺乏时间逐一阅读。需要一个轻量级工具，只需粘贴链接即可自动获取网页核心内容并生成结构化摘要，帮助用户快速筛选和吸收信息。

### 1.2 产品定位
一个**纯前端驱动的AI链接总结工具**，用户分享链接后，程序自动完成“抓取正文 → AI分析 → 生成总结”的流程，并在界面中清晰呈现结果，同时支持历史记录管理与导出。

### 1.3 目标用户
- 需要快速阅读大量资讯的内容消费者
- 希望保存网页摘要以便后续查阅的知识工作者
- 对AI应用感兴趣、想要体验轻量级总结工具的技术爱好者

---

## 2. 功能需求

### 2.1 核心流程（Happy Path）
1. 用户输入/粘贴一个有效的网页URL。
2. 点击“总结”或回车触发。
3. 系统显示加载状态，获取网页正文内容。
4. 调用AI模型接口，对正文进行智能总结。
5. 界面展示：
   - 网页标题、来源域名
   - 生成的总结（Markdown格式，含要点、关键结论等）
   - 原文关键词/标签（可选）
6. 本次总结自动存入本地历史记录。

### 2.2 功能模块详述

#### 2.2.1 URL输入与校验
- **输入框**：支持粘贴完整URL，带链接图标提示。
- **URL合法性检查**：前端校验是否以 `http://` 或 `https://` 开头，格式是否正确。
- **快捷操作**：支持回车键触发总结，提供“清空”按钮（×图标）。

#### 2.2.2 内容获取
- **代理抓取**：由于浏览器同源策略限制，前端无法直接抓取任意跨域网页。采用以下方案之一：
  - *方案A（轻量）*：使用公共CORS代理服务（如 `api.allorigins.win`）获取原始HTML，然后前端解析正文。
  - *方案B（生产）*：搭建自有后端服务或Serverless函数进行网页抓取与正文提取，前端通过API调取。
- **正文提取**：实现基于可读性算法（类似Mozilla Readability）的简化版，从HTML中提取标题、正文文本，剔除广告、导航等干扰内容。
- **超时与错误处理**：
  - 设置请求超时（如10秒），超时提示“内容获取超时，请稍后重试”。
  - 无法访问或非文本型页面（如PDF、直接下载链接）给出明确提示“不支持该类型的链接”。
  - 内容为空或过短（<100字符）时提示“无法提取有效正文”。

#### 2.2.3 AI总结生成
- **接口调用**：调用大语言模型API（如OpenAI、Claude、国内大模型等），通过前端直连或后端中转，确保API Key安全（生产环境不应暴露在前端）。
- **提示词设计**：默认提示词要求模型：
  - 生成简洁、准确的中文总结。
  - 结构化输出：包含【核心主题】、【关键要点】、【结论/启发】三部分。
  - 限制输出长度（例如300-500字）。
- **配置灵活性**：提供设置入口（可折叠），允许用户修改API端点、模型名称、提示词模板，以及是否显示原始上下文长度。
- **错误回退**：
  - API密钥无效或额度不足 → 提示“AI服务暂时不可用，请检查配置”。
  - 模型返回错误 → 显示具体错误信息并允许重试。
  - 内容过长超出token限制 → 自动截断至安全长度（如4000字符）后再发送。

#### 2.2.4 结果展示
- **总结卡片**：
  - 显示原链接标题（可点击跳转）、域名、抓取时间。
  - 总结内容以渲染后的Markdown展示（支持标题、列表、粗体等）。
  - 提供“复制总结”按钮，一键复制原始Markdown或纯文本。
- **原文预览**（可选）：折叠区域展示提取出的原始文本前200字，帮助用户对照。
- **重新总结**：允许用户点击“重新生成”以重新调用AI（可能因随机性获得不同总结）。

#### 2.2.5 历史记录
- **本地存储**：使用浏览器 `localStorage` 存储总结历史，按时间倒序排列。
- **历史列表**：侧边栏或独立弹窗展示，每条包含缩略标题、域名、日期。
- **操作**：
  - 点击历史项可回看完整总结。
  - 支持单条删除或一键清空全部历史。
  - （可选）导出为JSON文件备份。

#### 2.2.6 辅助功能
- **暗色模式/主题切换**：提供亮色/暗色主题，自动检测系统偏好，可手动切换。
- **键盘快捷键**：
  - `Ctrl+Enter` / `Cmd+Enter`：触发总结。
  - `Esc`：关闭历史面板或清空输入框。
- **响应式设计**：在移动端和桌面端均有良好体验。

---

## 3. 非功能需求

### 3.1 性能
- 初始页面加载时间 < 2秒（首屏内容）。
- 总结生成过程反馈及时：显示加载动画，避免用户误以为卡死。
- 历史记录列表渲染在条目<100时保持流畅。

### 3.2 安全与隐私
- **API Key 安全**：引导用户使用自己的Key，并明确提示存储于本地浏览器，不会上传至任何第三方服务器。若使用后端代理，需在后端环境变量中配置密钥。
- **内容安全**：不对用户输入的URL及获取的网页内容进行任何服务器端存储，所有处理尽量在客户端完成或即时清除。
- **输入消毒**：防止XSS，渲染用户内容时进行转义处理，Markdown渲染需过滤危险HTML标签。

### 3.3 兼容性
- 支持主流现代浏览器：Chrome 90+、Edge 90+、Safari 15+、Firefox 90+。
- 移动端浏览器适配良好。

### 3.4 可维护性
- 代码结构清晰，前端采用模块化JavaScript（或Vue/React组件思想）。
- 关键配置（默认API地址、提示词、超时时间）提取为常量，便于修改。
- 提供详细的README文档，说明运行方式与配置方法。

---

## 4. 界面设计概要

### 4.1 整体风格
- **设计理念**：温暖、简洁、纸张质感。采用浅驼色/米白背景，橙色作为强调色。
- **布局**：单栏居中卡片式布局，最大宽度约800px，留白充足。
- **动效**：按钮悬停微上浮、过渡平滑，加载使用呼吸态或骨架屏。

### 4.2 关键页面元素
1. **头部**：Logo + 应用名称“LinkSnap”，副标题简短说明。
2. **工具栏**：主题切换按钮、历史记录按钮（带条目计数徽章）、设置齿轮图标。
3. **主卡片**：
   - 顶部为URL输入区（带图标、输入框、行动召唤按钮）。
   - 中部动态区域：初始显示引导提示；加载中显示动画与文字；完成后显示总结卡片。
4. **历史侧板**（抽屉式）：从右侧滑出，半屏或全屏，列出历史项，支持点击查看与删除。
5. **设置面板**（弹出式）：包含API配置项、提示词编辑区、关于信息。

---

## 5. 技术方案建议

### 5.1 前端技术栈
- **纯HTML/CSS/JS** 或 **Vue 3 + Vite**（轻量SPA）
- **Markdown渲染**：`marked` 或 `markdown-it`
- **正文提取**：前端实现简化版Readability（基于正则与DOM分块），或使用 `@mozilla/readability` 库（需搭配DOMParser，但抓取仍需代理）
- **HTTP请求**：`fetch` API
- **本地存储**：`localStorage`

### 5.2 网络请求流程
[用户输入URL] 
    → 前端格式校验 
    → 调用CORS代理获取原始HTML 
    → 前端提取正文（Readability简化算法） 
    → 构造AI请求体（截断、拼接提示词） 
    → 调用AI API 
    → 接收总结结果 
    → 渲染Markdown并存入本地历史
### 5.3 关键接口依赖
| 接口用途       | 示例服务                          | 备注                             |
|----------------|-----------------------------------|----------------------------------|
| 网页代理抓取   | `https://api.allorigins.win/raw?url=...` | 需处理编码和可能被屏蔽的风险     |
| AI文本生成     | OpenAI Chat Completions API       | 也可替换为任何兼容OpenAI格式的API |

---

## 6. 风险与边界条件

- **CORS代理稳定性**：免费代理可能有速率限制或服务中断，建议提供多个备选代理或自建Serverless抓取函数。
- **AI费用控制**：依赖用户自行提供有效API Key，产品本身不承担费用。需在界面明确提示用户控制使用频率。
- **大型网页与动态页面**：纯HTML抓取无法获取需JS渲染的SPA内容，此场景下总结质量受限，应提示用户。
- **法律合规**：用户应确保有权读取和总结目标网页内容，工具不承担版权责任。

---

## 7. 未来扩展（规划）

- 支持批量链接总结
- 支持上传本地PDF/文档并总结
- 多语言总结选择
- 阅读历史云同步（需后端用户系统）
- 浏览器扩展，一键总结当前页面
- 分享总结卡片生成图片功能

---

> **文档维护说明**：本文档将随产品迭代持续更新，所有功能优先级通过版本计划会议商定。